import React from 'react';
import styled from 'styled-components';

const LogoHeaderContainer = styled.div`
  display: flex;
  align-items: center;
`;

const LogoHeaderImageBox = styled.div`
  max-width: 5rem;
  margin-right: 1rem;
`;

export default { LogoHeaderContainer, LogoHeaderImageBox };
