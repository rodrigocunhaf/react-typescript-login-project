import { DefaultTheme } from 'styled-components';
import { ProjectColors } from '../../../../../../global/configs/colors';

export const whiteSmall: DefaultTheme = {
  fontSize: '1rem',
  fontColor: ProjectColors.WHITE,
};
